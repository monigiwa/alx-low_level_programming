#include "main.h"

/**
 * main - prints the alphabet in lower case
 *
 * Description: prints the alphabet in lower case
 *
 * Return: 0 (Success)
 */

int main(void)
{
	_putchar ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	return (0);
}
